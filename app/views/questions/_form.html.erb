<%= render 'shared/errors' , obj: @question %>
  <div class='row'>
    <div class='col-xs-12'>

      <%= form_for(@question, :html=> {class: "form-horizontal", role: "form"}) do |f| %>
        <div class="form-group">
          <div class="control-label col-sm-2">
            <%= f.label :question %>
          </div>
          <div class="col-sm-8">
            <%= f.text_field :question, class: "form-control" , placeholder: "Enter Question" , autofocus: true %>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <%= f.submit class: 'btn btn-primary btn-lg' %>
          </div>
        </div>
        <% end %>
          <div class="col-xs-4 col-xs-offset-4">
            [ <%= link_to "Cancel request and return to survey listing" , surveys_path %> ]
          </div>
    </div>
  </div>